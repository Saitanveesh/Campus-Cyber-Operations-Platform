# Build plan

The monitor is implemented as a Windows-native defensive observability platform. Development proceeds through capture/session truth, protocol and flow parsing, asset and performance intelligence, topology, defensive IDS integration, alerting, evidence, endpoint visibility for explicitly enrolled lab hosts, and installer hardening.

The live UI remains strictly monochrome. Every live record is tied to the active session. Historical data is never used as a live fallback.
